# /engineering-os:init-project

Initialize a new Git project with `.engineering-os/`, project memory files,
`AGENTS.md` contract, and Claude adapter (agents, skills, commands). Equivalent
to `bootstrap --apply --adapter claude`.
