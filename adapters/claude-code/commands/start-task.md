# /engineering-os:start-task

Use before editing. Run the portable `start-task` command with task title,
owner, acceptance criteria, and explicit target project. Report the returned
task ID in subsequent evidence, incident, lesson, and handoff records.
