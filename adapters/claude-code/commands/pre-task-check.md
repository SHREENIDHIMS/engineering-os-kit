# /engineering-os:pre-task-check

Run before editing code. Confirms Engineering OS is initialized, checks for
handoff blockers, and validates record integrity. Use `--allow-no-active-task`
only in CI.
