# /engineering-os:handoff

Before assigning work to another agent, create a portable handoff with exact
repository-relative `path:line` locations, command evidence, remaining risk,
and the next agent's first action.
