# /engineering-os:show-task

Load full task context before pickup: changed `path:line` locations, handoff
state, incidents, and enforced lessons. Run after `accept-handoff` or when
resuming an in-progress task.
